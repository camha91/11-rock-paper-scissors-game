import { combineReducers } from "redux";
import RockPaperScissorsGameReducer from "../redux/RockPaperScissorsGameReducer";

const rootReducer = combineReducers({
  RockPaperScissorsGameReducer,
});

export default rootReducer;
